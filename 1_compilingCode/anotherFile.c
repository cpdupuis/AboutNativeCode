char* getMessageFromOtherFile(int whichOne) {
    switch (whichOne) {
        case 0:
            return "zero";
        case 1:
            return "this is message number one";
        default:
            return "this is the default message";
    }
 
}
